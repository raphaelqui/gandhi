{
  "name": "gandhi-restaurant",
  "version": 2,
  "public": true,
  "buildCommand": "npm run build",
  "devCommand": "npm run dev",
  "env": {
    "NEXT_PUBLIC_GOOGLE_PLACES_API_KEY": {
      "description": "Google Places API Key für Google Maps",
      "required": true
    },
    "WHATSAPP_PHONE_NUMBER": {
      "description": "WhatsApp Telefonnummer für Reservierungen",
      "required": true
    }
  },
  "functions": {
    "pages/api/**/*.ts": {
      "memory": 512,
      "maxDuration": 60
    }
  },
  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        {
          "key": "X-Content-Type-Options",
          "value": "nosniff"
        },
        {
          "key": "X-Frame-Options",
          "value": "SAMEORIGIN"
        },
        {
          "key": "X-XSS-Protection",
          "value": "1; mode=block"
        }
      ]
    }
  ],
  "regions": ["iad1", "cle1", "fra1"],
  "framework": "nextjs"
}
